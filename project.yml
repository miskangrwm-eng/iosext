name: WrapperApp

targets:
  WrapperApp:
    type: application
    platform: iOS
    sources:
      - path: .
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.example.wrapper
      GENERATE_INFOPLIST_FILE: YES
